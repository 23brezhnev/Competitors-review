# --- Web / admin ---
fastapi>=0.110
uvicorn[standard]>=0.27
sqladmin>=0.16
sqlalchemy>=2.0
psycopg2-binary>=2.9      # Postgres (Supabase) driver
python-dotenv>=1.0
bcrypt>=4.1
itsdangerous>=2.1

# --- Collectors ---
requests>=2.31
telethon>=1.34            # Telegram (public channels via MTProto)
feedparser>=6.0           # RSS
beautifulsoup4>=4.12      # website scraping / change detection
google-play-scraper>=1.2  # Google Play reviews & ratings

# --- LLM (summarization) ---
openai>=1.14              # DeepSeek uses the OpenAI-compatible API
